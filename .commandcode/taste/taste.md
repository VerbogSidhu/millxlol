# Taste

- Expects the agent to read the project's AGENTS.md (project instructions) before making changes. Confidence: 0.5
- When adding items to a data collection, expects the agent to also update derived/duplicated values elsewhere (e.g., a hardcoded counter/denominator in HTML), not just the obvious list. Confidence: 0.7
- Prefers new entries appended to the end of an existing list/array rather than inserted elsewhere. Confidence: 0.55
